# ── Core ML ────────────────────────────────────────────────────
transformers>=4.43.0,<4.52.0
accelerate>=0.33.0
bitsandbytes>=0.43.0
peft>=0.12.0

# ── sentence-transformers pinned to avoid torchcodec/FFmpeg crash ──
# Versions >= 3.x pull in torchcodec which requires FFmpeg shared libs
# not available in Kaggle/standard GPU envs. 2.7.0 is the last clean version.
sentence-transformers==2.7.0

# ── Embeddings & retrieval ──────────────────────────────────────
FlagEmbedding>=1.2.0
qdrant-client>=1.9.0
fastembed>=0.3.6

# ── Agent & LLM orchestration ───────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.2.0
langsmith>=0.1.0

# ── Evaluation ─────────────────────────────────────────────────
ragas>=0.1.14
datasets>=2.20.0

# ── UI ─────────────────────────────────────────────────────────
gradio>=4.0.0,<5.0.0

# ── Document processing ─────────────────────────────────────────
networkx>=3.0
PyMuPDF>=1.24.0
pdfplumber>=0.11.0
beautifulsoup4>=4.12.0
python-docx>=1.1.0

# ── Utilities ──────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0
pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.8.0
tqdm>=4.66.0

# ── Dev tools (not installed in Kaggle/production) ─────────────
pytest>=8.0.0
black>=24.0.0
ruff>=0.4.0
